{
  "name": "microservices-platform",
  "version": "1.0.0",
  "description": "Production-Grade API Gateway & Microservices Platform",
  "private": true,
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "dev:all": "concurrently -c \"cyan,magenta,yellow,blue,green\" -n \"GATE,AUTH,USER,ORDR,INVY,NOTF\" \"npm run dev -w services/api-gateway\" \"npm run dev -w services/auth-service\" \"npm run dev -w services/user-service\" \"npm run dev -w services/order-service\" \"npm run dev -w services/inventory-service\" \"npm run dev -w services/notification-service\"",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "lint": "eslint . --ext .js",
    "format": "prettier --write .",
    "test": "jest"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Kavish Vachheta",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}